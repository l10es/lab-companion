<%= form_with(model: post_status, local: true) do |form| %>
  <% if post_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post_status.errors.count, "error") %> prohibited this post_status from being saved:</h2>

      <ul>
      <% post_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
