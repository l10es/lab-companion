<div class="parallax-container">
  <div class="parallax"><%= image_tag "wellcome_wallpaper.jpg" %></div>
</div>
<div class="section white">
  <div class="row container">
    <h2 class="header">Wellcome to Lab Companion</h2>
    <p class="grey-text text-darken-3 lighten-3">
      Assist your laboratory management. Please login to the system. <br />
      Have a grate day!!
    </p>
    <% if !member_signed_in? %>
      <a class="waves-effect waves-light btn" href="<%= new_member_session_path %>">Log&nbsp;in</a>
      <a class="waves-effect waves-light btn" href="<%= new_member_registration_path %>">Sign&nbsp;up</a>
    <% end %>
  </div>
</div>